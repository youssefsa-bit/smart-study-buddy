import 'dart:ui';

class AppColors{
  static const Color background = Color(0xFF0D0D0D); // Main background color (very dark)
  static const Color surface = Color(0xFF1A1C20); // Card background color
  static const Color surfaceHighlight = Color(0xFF25282D); // Color for pressed states or highlights
  // --- Primary & Accents ---
  static const Color primaryBlue = Color(0xFF246BFD); // Primary blue (upload button, spark icon)
  static const Color mcqOrange = Color(0xFFFF7A00);// Bright orange (MCQ Quiz card)
  static const Color flashcardGreen = Color(0xFF00C566); // Green (Flashcards card)
  static const Color summarizePurple = Color(0xFF8A2BE2); // Purple (Summarize card)
// --- Text Colors ---
  static const Color textPrimary = Color(0xFFFFFFFF);// Main text (White)
  static const Color textSecondary = Color(0xFFA0A0A0); // Secondary text (Light grey)
// --- Borders & Dividers ---
  static const Color border = Color(0xFF2A2A2A); // Subtle border color for separating elements
}